<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Splash Screen Theme. -->
    <style name="Theme.AppSplash" parent="Theme.SplashScreen">
        <item name="backgroundColor">@color/primary_500</item>
        <item name="windowSplashScreenBackground">@color/primary_500</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_no_background</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.Kauf</item>

        <!-- Status bar and Nav bar configs -->
        <item name="android:statusBarColor" >@color/primary_500</item>
        <item name="android:navigationBarColor">@color/primary_500</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>